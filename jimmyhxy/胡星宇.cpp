#include<iostream>
using namespace std;
int r,g,b;
int make(int m)
{
    return m+(r-m)/3+(g-m)/3+(b-m)/3;
}    
int main()
{
    int ans;
    cin<<r<<g<<b;
    int t=min(min(a,b),c);
    ans=max(max(test(t),test(max(t-1,0))),test(max(t-2,0)))
    cout<<ans;
    return 0;
}
